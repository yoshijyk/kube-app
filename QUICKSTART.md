# GitHub Copilot Agents クイックスタートガイド

このガイドでは、GitHub Copilot Agentsをすぐに使い始めるための基本的な例を紹介します。

## 基本的な使い方

### 1. Agentsに質問する

```
「このリポジトリの構成を教えてください」
```

Agentsは、リポジトリの内容を分析して説明してくれます。

### 2. コードの改善を依頼する

```
「vproappdep.yamlにリソース制限を追加してください」
```

Agentsは、適切なリソース制限を追加したファイルを更新します。

### 3. ドキュメントの作成を依頼する

```
「各デプロイメントファイルの説明をコメントとして追加してください」
```

## よく使う依頼の例

### セキュリティ関連

```
「すべてのデプロイメントにセキュリティコンテキストを追加してください」

「シークレット管理のベストプラクティスを実装してください」

「非rootユーザーでコンテナを実行するように設定してください」
```

### パフォーマンス最適化

```
「vproappデプロイメントにHorizontalPodAutoscalerを追加してください」

「各コンテナに適切なリソースリクエストとリミットを設定してください」

「データベースの永続ボリュームを最適化してください」
```

### 可用性の向上

```
「vproappデプロイメントにliveness probeとreadiness probeを追加してください」

「PodDisruptionBudgetを設定して、ローリングアップデート時の可用性を確保してください」

「データベースのバックアップ戦略を実装してください」
```

### CI/CD

```
「Kubernetesマニフェストを検証するGitHub Actionsワークフローを作成してください」

「デプロイメントを自動化するGitHub Actionsワークフローを作成してください」

「セキュリティスキャンをCI/CDパイプラインに組み込んでください」
```

### 環境管理

```
「開発環境と本番環境用のKustomize設定を作成してください」

「環境変数を管理するためのConfigMapを作成してください」

「環境ごとに異なるリソース設定を適用してください」
```

## ステップバイステップの例

### 例1: 本番環境へのデプロイ準備

**ステップ1: リソース制限を追加**
```
「vproappdep.yamlにリソースリクエストとリミットを追加してください。
CPU: 500m request, 1000m limit
メモリ: 512Mi request, 2Gi limit」
```

**ステップ2: ヘルスチェックを追加**
```
「vproappdep.yamlにliveness probeとreadiness probeを追加してください。
HTTPのGETリクエストで/healthと/readyエンドポイントをチェックしてください」
```

**ステップ3: オートスケールを設定**
```
「vproapp用のHorizontalPodAutoscalerを作成してください。
最小レプリカ: 3, 最大レプリカ: 10
CPU使用率70%でスケールアウト」
```

**ステップ4: PodDisruptionBudgetを設定**
```
「vproapp用のPodDisruptionBudgetを作成してください。
最低2つのポッドが常に利用可能であること」
```

### 例2: モニタリングの追加

**ステップ1: ServiceMonitorを作成**
```
「Prometheus用のServiceMonitorを作成してください。
vproappのメトリクスを/metricsエンドポイントから収集」
```

**ステップ2: Grafanaダッシュボード設定**
```
「vproappのGrafanaダッシュボード用のConfigMapを作成してください。
HTTPリクエスト数、レスポンスタイム、エラー率を表示」
```

### 例3: セキュリティ強化

**ステップ1: セキュリティコンテキストを追加**
```
「すべてのデプロイメントに以下のセキュリティコンテキストを追加してください：
- 非rootユーザーで実行
- 権限昇格を無効化
- すべてのCapabilityをdrop」
```

**ステップ2: NetworkPolicyを作成**
```
「vproappとvprodbの間の通信を制限するNetworkPolicyを作成してください」
```

**ステップ3: シークレット管理の改善**
```
「External Secrets Operatorを使用してシークレットを管理する設定を作成してください」
```

## トラブルシューティング

### ポッドが起動しない場合

```
「vproappのポッドが起動しない原因を調査してください。
以下のエラーが出ています：
[エラーメッセージをここに貼り付け]」
```

### イメージのプルに失敗する場合

```
「イメージプルエラーを解決してください。
ImagePullSecretsの設定を確認して、必要なら追加してください」
```

### リソース不足の場合

```
「クラスターのリソース使用状況を確認して、
リソースリクエストを最適化してください」
```

## ヒント

1. **明確に依頼する**: 具体的な要件を明確に伝えることで、より正確な結果が得られます。

2. **段階的に進める**: 大きな変更は小さなステップに分けて依頼します。

3. **確認する**: Agentsの提案を確認し、必要に応じて調整を依頼します。

4. **学ぶ**: Agentsが生成したコードから、ベストプラクティスを学びます。

5. **繰り返す**: 最初の結果が完璧でなくても、追加の指示で改善できます。

## 次のステップ

詳細な使い方やより高度な例については、[AGENTS.md](./AGENTS.md)をご覧ください。

実践的な設定例は、[examples/](./examples/)ディレクトリにあります。
